const fs = require('fs');
const yaml = require('js-yaml');

const path = 'config/tools.yaml';
let doc;
try {
  doc = yaml.load(fs.readFileSync(path, 'utf8'));
} catch (e) {
  console.error('Error loading YAML:', e);
  process.exit(1);
}

const updates = {
  'qr-code-generator': {
    seo: {
      title: 'QR Code Generator — Create Custom QR Codes',
      description: 'Generate high-resolution, custom QR codes for URLs, text, WiFi passwords, and vCards. Download in PNG or SVG formats for print and digital marketing.',
      keywords: ['qr code generator', 'create custom qr code', 'generate wifi qr code', 'qr code maker png', 'high resolution qr code'],
      tldr: 'Generate high-resolution, scannable QR codes for URLs, WiFi networks, and digital business cards.',
      entity_definition: 'The QR Code Generator is a digital encoding utility. A Quick Response (QR) code is a two-dimensional matrix barcode that stores data—such as URLs, text, or VCard contact info—as a grid of black and white squares. This tool utilizes matrix encoding algorithms to instantly translate alphanumeric input into a strictly formatted visual grid that is instantly readable by smartphone cameras, outputting scalable vector (SVG) or raster (PNG) image files.'
    },
    faqs: [
      {
        question: 'Do QR codes expire?',
        answer: 'No. The QR codes generated by this tool are "Static." The data is hardcoded directly into the squares of the image. As long as the URL or text you encoded remains valid, the QR code will work forever.'
      },
      {
        question: 'What is error correction?',
        answer: 'QR codes have built-in redundancy (Error Correction). This means that even if a portion of the QR code is damaged, dirty, or covered up by a logo, the scanner can still mathematically reconstruct the missing data.'
      },
      {
        question: 'Why should I download as SVG?',
        answer: 'SVG (Scalable Vector Graphics) is a mathematical image format. Unlike a PNG, an SVG can be scaled up to the size of a massive billboard without ever losing quality or becoming pixelated.'
      }
    ]
  },
  'xml-to-csv': {
    seo: {
      title: 'XML to CSV Converter — Parse and Flatten XML',
      description: 'Convert nested XML data into a flat CSV spreadsheet instantly. Perfect for migrating web feeds and databases into Microsoft Excel or Google Sheets.',
      keywords: ['xml to csv converter', 'parse xml to excel', 'flatten xml data', 'xml spreadsheet converter', 'convert xml feed to csv'],
      tldr: 'Algorithmically flatten complex, nested XML data into a clean CSV spreadsheet.',
      entity_definition: 'The XML to CSV Converter is a data transformation utility. Extensible Markup Language (XML) is a hierarchical, tree-based data format commonly used in RSS feeds and legacy SOAP APIs. However, data analysts often require flat, tabular formats like CSV (Comma-Separated Values) for spreadsheet processing. This tool parses the DOM tree of an XML payload, intelligently flattening nested nodes and extracting attributes into a structured two-dimensional grid of rows and columns.'
    },
    faqs: [
      {
        question: 'How are nested XML tags handled?',
        answer: 'Converting a 3D tree (XML) into a 2D table (CSV) requires flattening. If an XML node has deeply nested children, the converter will typically concatenate the tag names (e.g., `parent_child_subchild`) to create a unique CSV column header.'
      },
      {
        question: 'What happens to XML attributes?',
        answer: 'XML attributes (like `<user id="123">`) are extracted alongside the text content. The converter usually creates a separate column specifically for the attribute, often named `user_id` or similar.'
      },
      {
        question: 'Is my XML data secure?',
        answer: 'Yes. The XML parsing and CSV generation occur entirely client-side within your browser. Your data is never uploaded to a remote server.'
      }
    ]
  },
  'csv-to-xml': {
    seo: {
      title: 'CSV to XML Converter — Build XML Trees from CSV',
      description: 'Convert flat CSV spreadsheets into structured XML data. Perfect for importing product feeds, databases, and spreadsheets into enterprise software systems.',
      keywords: ['csv to xml converter', 'spreadsheet to xml', 'convert csv to xml feed', 'generate xml from csv', 'data feed converter'],
      tldr: 'Transform flat CSV spreadsheet rows into deeply structured XML data feeds.',
      entity_definition: 'The CSV to XML Converter is a data integration utility. Comma-Separated Values (CSV) is the universal format for exporting database tables and Excel spreadsheets, but many enterprise systems (like inventory management or older SOAP APIs) strictly require Extensible Markup Language (XML) payloads. This tool reads the flat CSV matrix, using the first row as element tags, and wraps every subsequent row into a structured, hierarchical XML document.'
    },
    faqs: [
      {
        question: 'How are the XML tags generated?',
        answer: 'The converter uses the very first row of your CSV file (the headers) as the names for the XML tags. For example, a CSV column named "Price" will become `<Price>` in the resulting XML.'
      },
      {
        question: 'What if my CSV has no headers?',
        answer: 'If your CSV lacks a header row, the tool will generate generic tags (like `<column1>`, `<column2>`) for each data point to maintain structural integrity.'
      },
      {
        question: 'Can this create nested XML?',
        answer: 'No. A CSV is inherently a flat, two-dimensional format. Converting it to XML will result in a "shallow" XML tree, where each row is a parent node containing flat child nodes for each column.'
      }
    ]
  },
  'mobile-meta-gen': {
    seo: {
      title: 'Mobile App Meta Tag Generator — iOS & Android Links',
      description: 'Generate Apple Smart App Banner and Android App Link meta tags. Seamlessly route mobile web users to your native app on the App Store or Google Play.',
      keywords: ['mobile app meta tags', 'apple smart app banner', 'android app link generator', 'ios app banner code', 'deep linking meta tags'],
      tldr: 'Generate Apple Smart App Banners and Android App Links to route web users to your native app.',
      entity_definition: 'The Mobile App Meta Tag Generator is a mobile acquisition utility. When a user visits a website on a mobile device, companies often want to push them into their native application. This tool generates platform-specific HTML metadata—like Apple\'s `<meta name="apple-itunes-app">`—which explicitly instructs the Safari or Chrome browser to display a native "Smart App Banner" at the top of the screen, linking directly to the App Store or deep-linking into the app if it\'s already installed.'
    },
    faqs: [
      {
        question: 'What is a Smart App Banner?',
        answer: 'A Smart App Banner is a native UI element rendered by iOS Safari. It provides a clean, unobtrusive banner at the top of a webpage prompting the user to either "View" the app in the App Store or "Open" the app if it is already installed.'
      },
      {
        question: 'Does Android support Smart App Banners?',
        answer: 'Android Chrome does not natively support the exact same meta tag as Apple. Instead, Android relies on Web App Manifests or custom JavaScript implementations to display equivalent "Add to Home Screen" or app install prompts.'
      },
      {
        question: 'What is the "app-argument" parameter?',
        answer: 'The `app-argument` provides a specific URL or deep-link context. If the user already has your app installed, clicking the banner will open the app and pass this argument, allowing you to route them directly to the specific product or page they were viewing on the web.'
      }
    ]
  },
  'webhook-visualizer': {
    seo: {
      title: 'Webhook Visualizer & Tester — Inspect HTTP Hooks',
      description: 'Test, inspect, and visualize incoming HTTP webhooks. Generate a unique endpoint URL to catch payloads from Stripe, GitHub, or Shopify instantly.',
      keywords: ['webhook visualizer', 'test webhooks online', 'inspect http post', 'webhook catcher', 'stripe webhook tester'],
      tldr: 'Generate a unique endpoint URL to catch, inspect, and debug incoming HTTP webhook payloads.',
      entity_definition: 'The Webhook Visualizer is an API integration and debugging utility. A Webhook is a user-defined HTTP callback; when an event occurs in a third-party application (like a payment succeeding in Stripe), that app sends an HTTP POST payload to your server. This tool provisions a temporary, secure URL endpoint to "catch" these payloads, allowing developers to visually inspect the raw JSON body, HTTP headers, and signature hashes before writing the backend code to process them.'
    },
    faqs: [
      {
        question: 'What is the difference between an API and a Webhook?',
        answer: 'An API requires you to actively "pull" or request data from a server. A Webhook is a "push" mechanism; the third-party server automatically sends the data to your endpoint the exact millisecond an event occurs.'
      },
      {
        question: 'How do I test a Stripe webhook?',
        answer: 'Copy the unique URL generated by this tool and paste it into your Stripe Developer Dashboard as a Webhook Endpoint. When you trigger a test payment, this tool will display the exact JSON payload Stripe sends.'
      },
      {
        question: 'Is my webhook data public?',
        answer: 'Webhook payloads can contain sensitive data (like customer emails). While the temporary URL is cryptographically random and difficult to guess, it is highly recommended to use test data (not production data) when using public webhook catchers.'
      }
    ]
  }
};

let updatedCount = 0;
doc.tools.forEach(tool => {
  if (updates[tool.slug]) {
    const data = updates[tool.slug];
    tool.seo = tool.seo || {};
    tool.seo.title = data.seo.title;
    tool.seo.description = data.seo.description;
    tool.seo.keywords = data.seo.keywords;
    tool.seo.tldr = data.seo.tldr;
    tool.seo.entity_definition = data.seo.entity_definition;
    
    // Add FAQs
    tool.faqs = data.faqs;
    updatedCount++;
  }
});

fs.writeFileSync(path, yaml.dump(doc, { lineWidth: -1 }), 'utf8');
console.log('Successfully updated', updatedCount, 'tools in tools.yaml for Batch 35');
